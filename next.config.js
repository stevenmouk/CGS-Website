/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  reactStrictMode: true,
  images: {
    domains: ["cdn.asaha.com"],
  },
};

module.exports = nextConfig;
